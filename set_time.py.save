import datetime
import time
import serial

ser = serial.Serial('/dev/serial0', 9600, timeout=1)
time.sleep(1)
               0000000000000000000000001000x

ser.write(dateSt.encode())

ser.close()

